{
  "name": "rbyers-pooltemp",
  "description": "Simple Node.js app for logging pool info",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "author": "Rick Byers",
  "repository": {
    "type": "git",
    "url": ""
  },
  "engines": {
    "node": ">=4.3.2"
  },
  "scripts": {
    "deploy": "gcloud app deploy",
    "start": "node app.js",
    "lint": "samples lint",
    "pretest": "npm run lint",
    "system-test": "samples test app",
    "test": "npm run system-test",
    "e2e-test": "samples test deploy"
  },
  "dependencies": {
    "express": "latest",
    "node-fetch": "latest"
  },
  "devDependencies": {
    "@google-cloud/nodejs-repo-tools": "1.4.17"
  },
  "cloud-repo-tools": {
    "test": {
      "app": {
        "msg": "Hello, world!"
      }
    },
    "requiresKeyFile": true,
    "requiresProjectId": true
  }
}
